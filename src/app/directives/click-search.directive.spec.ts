import { ClickSearchDirective } from './click-search.directive';

describe('ClickSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickSearchDirective();
    expect(directive).toBeTruthy();
  });
});

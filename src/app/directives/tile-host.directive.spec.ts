import { TileHostDirective } from './tile-host.directive';

describe('TileHostDirective', () => {
  it('should create an instance', () => {
    const directive = new TileHostDirective();
    expect(directive).toBeTruthy();
  });
});

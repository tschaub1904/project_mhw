import { NumberSymbol } from '@angular/common';

export interface Defense {
  base: number,
  max: number,
  augmented: number
}

export interface Resistances {
  fire: number,
  water: number,
  ice: number,
  thunder: number,
  dragon: number
}